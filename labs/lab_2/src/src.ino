/*
    Himadri Saha

    src.ino
        - Choosing Track K

    TODO
        Project Phase
            - A.1 starting from
            starter/k1-audio-capture/, initialize the on-board PDM microphone, capture a buffer of
            audio samples, and report over Serial that audio is being captured (samples read and a
            simple level/amplitude per buffer). This is the sketch you will later point the Edge Impulse
            data forwarder at. Deliverable: the sketch and a Serial Monitor screenshot of live values
            (≥ 10 lines for Track W; several capture reports for Track K).
        
            - A.2 describe the MFE (Mel-filterbank energy) fea-
            ture pipeline Edge Impulse uses — what an MFE/spectrogram represents, why a frequency-
            domain representation suits speech, and the shape (time × frequency bins) of the feature
            block the CNN sees as its input “image.”
        
        Measure Phase
        Doc Phase

*/

void setup() {

}

void loop() {

}